<section>

<div class="container">
  
  <h1>Book Gallery</h1>
  {{#if prevPage}}
    <div class = "col">
      <p>{{prevPage}}</p>
      <a href="/books/{{queryStringPrev}}">Prev Page</a>
    </div>
  {{/if}}
    <div class="col">
    {{#each books}}
      {{> books}}
    {{/each}}
    </div>
  {{#if nextPage}}
    <div class = "col">
      <p>{{nextPage}}</p>
      <a href="/books/{{queryStringNext}}">Next Page</a>
    </div>
  {{/if}}
</div>

</section> 

