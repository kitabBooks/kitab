
<div class=" col-sm-12 col-md-6 col-md-4">

  <div class="thumbnail">
    {{!-- <img class="book-img" src={{this.img}} alt=""> --}}
    <div class="caption">
      <h2 class="book-title">{{this.title}}</h2>
      <h3 class="book-author">{{this.author}}</h3>
      <p class="book-description">{{this.description}}</p>
      
    <div class="row">
        <div class="col-xs-4">
          <form action="/books/{{this._id}}" >
        <button type="submit" class="btn btn-light  interest-btn" role="button">Details</button>
        </form>
           {{!-- <a href="/books/{{this._id}}" class=" btn btn-light interest-btn " role="button">Details</a> --}}
            
        </div>
        <div class="col-xs-4">
           <form action="/interests/{{this._id}}" method="POST">
        <button type="submit" class="btn btn-light  interest-btn" role="button">Interest</button>
        </form>
            
        </div>
        <div class="col-xs-4">
          <form action="/posts/{{this._id}}" method="POST">
        <button type="submit" class="btn btn-light  interest-btn" role="button">Post</button>
        </form>
            
        </div>
    </div>

    </div>
    
  </div>
</div>
